name: Deploy use case demo static app

on:
  push:
    branches:
      - main

  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    env:
      APP_ROOT: stati-demo-app-deployment

    steps:

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies and build
        run: |
          cd ${{env.APP_ROOT}}
          npm install
          npm run build

      - name: Setup pages
        uses: actions/configure-pages@v5

      - name: Upload artifacts
        uses: actions/upload-pages-artifact@v3
        with:
          path: '${{env.APP_ROOT}}/build'

      - name: Deploy to github pages
        id: deployment
        uses: actions/deploy-pages@v4
